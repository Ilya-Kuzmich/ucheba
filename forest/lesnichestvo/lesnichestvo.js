class TaxiStation{
    constructor(needlistTrees = false){
        this.xvoiaTrees=[];
        if (needlistTrees){
            this.listTrees =[]
        }
    }
}

addxvoiaTress(trees);{
    this.xvoiaTrees.push(trees)
}

addlistTress(trees);{
    if (this.listTress) {
        this.listTress.push(trees);
    }else{
        console.log('net listtrees')
    }

    thicknessFilter(thickness);{
        this.rreeesPool.find((tree)=>tree.getthickness
    )
    }
}

module.exports = lesnichestvo;